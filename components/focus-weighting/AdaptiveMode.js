// Owner: Jonathan Lugo
export default function AdaptiveMode() {
  // TODO(J): nudge the student when a task has likely hit diminishing
  // returns. Doc scopes v1 as a fixed time-threshold-per-effort-level
  // heuristic (not real ML/personalization) — real adaptivity is a
  // stretch goal for later.
  return (
    <div>
      <p>Adaptive mode: interval based on task weight</p>
    </div>
  )
}
